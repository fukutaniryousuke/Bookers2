<div class="container">
  <div class="row">
      <div class="mx-auto">
   <h2>Editing Book</h2>

   <% if @book.errors.any? %>
     <%= @book.errors.count %>error prohibited this obj from being:
    <ul>
     <% @book. errors.full_messages.each do |message| %>
      <li><%= message %></li>
     <% end %>
    </ul>
   <% end %>


     <%= form_with model: @book, url: book_path(@book.id), method: :patch  do |book| %>

   <p>Title</p>
     <%= book.text_field :title %>

   <p>Opinion</p>

          <%= book.text_area :body %>
        <% if @book.user == current_user %><br>
          <%= button_tag type: "submit", class: "btn btn-success" do %>
          <i class="fas fa-pen"></i>Update Book
          <% end %>
        <% end %>
     <% end %>
     <%= link_to "Show", book_path(@book.id) %> |
     <%= link_to "Back", books_path %>
    </div>
  </div>
</div>